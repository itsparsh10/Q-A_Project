/**
 * Legacy entry: MongoDB init. Schema is now applied via Supabase SQL.
 * Run: supabase/migrations/001_initial_schema.sql in the Supabase SQL Editor.
 */
export async function initDatabase() {
  console.log('Use Supabase: run supabase/migrations/001_initial_schema.sql in the SQL Editor.');
}
